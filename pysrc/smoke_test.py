
import set_path
import torch

set_path.append_sys_path()
import bridge
import pyrela
import bridgelearn
import bridgeplay

print(dir(bridge))
print(dir(bridgelearn))
print(dir(pyrela))
print(dir(bridgeplay))

