
import set_path
import torch

set_path.append_sys_path()
import bridge
import bridgelearn
import rela
import bridgeplay

print(dir(bridge))
print(dir(bridgelearn))
print(dir(rela))
print(dir(bridgeplay))

